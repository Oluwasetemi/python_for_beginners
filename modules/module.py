import converters
from max import maxValue

# from converters import convertKilosToPounds

print(converters.convertPoundsToKilos(70))
print('--' * 10)
numbers = [3, 6, 2, 8, 4, 10]
print(maxValue(numbers))
